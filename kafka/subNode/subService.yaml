apiVersion: v1
kind: Service
metadata:
  name: nodejs-kafka-subscriber
spec:
  ports:
  - name: "toport8080"
    port: 8080
    targetPort: 8080
  selector:
    app: nodejs-kafka-subscriber-app
  type: ClusterIP